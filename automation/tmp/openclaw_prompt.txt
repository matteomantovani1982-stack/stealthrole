You are testing a FastAPI backend for CareerOS (StealthRole).

**Feature under test**: Fix for `avg_shadow_confidence` semantic null handling

**What was changed**:
1. `GET /api/v1/analytics/summary` — `avg_shadow_confidence` now returns `null` (JSON null) instead of `0.0` when the user has no shadow applications. This distinguishes "no data available" from "calculated average is zero."
2. The check was changed from `if avg_confidence` (falsy) to `if avg_confidence is not None` (explicit None check), which also correctly handles the edge case where the real average is exactly `0.0`.

**Files changed**:
1. `app/api/routes/analytics.py` — line 69: `avg_shadow_confidence` returns `None` when no confidence data exists
2. `tests/test_analytics_ext.py` — line 116: updated assertion from `== 0.0` to `is None`

**Test results**: 403 passed, 1 skipped (unit tests, excluding e2e; test_uploads.py excluded due to pre-existing missing dependency)

**Verify**:
1. TRUST: No new endpoints or parameters — this is a bugfix to existing response shape
2. TRUST: User isolation unchanged — all queries still filter by `current_user_id`
3. INTELLIGENCE: `None` vs `0.0` is the correct semantic distinction — `0.0` means "the average is zero", `null` means "no data to average"
4. INTELLIGENCE: Using `is not None` instead of falsy check correctly handles the edge case where avg confidence is exactly `0.0` (which would be falsy but is valid data)
5. INTELLIGENCE: All 5 analytics extension tests pass, including the updated empty shadows test
6. Edge case: User with shadows whose avg confidence is exactly `0.0` now correctly returns `0.0` (not `None`)
7. Edge case: User with zero shadows returns `null` for `avg_shadow_confidence`

Rate this implementation: PASS / FAIL / WEAK TRUST / WEAK INTELLIGENCE